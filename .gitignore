__pycache__/
*.pyc
.DS_Store

# Recorded audio: real files, committed deliberately rather than by accident.
# Recordings themselves ARE wanted in the repo (they are the point), so this
# only ignores the scratch formats a browser produces before conversion.
web/audio/human/*.tmp
